n = int(input())

for i in range(1, n + 1):
  spaces = n - i
  stars = i
  row = ((" ") * spaces) + (("* ") * stars)
  print(row)

for i in range(1, n):
  spaces = i
  stars = n - i
  row = ((" ") * spaces) + (("* ") * stars)
  print(row)
